1. git clone https://github.com/Iratsareva/restaurant1.git
2. docker compose up -d --build





### 1. Demo REST API (Основной сервис)
URL: http://localhost:8081

Что проверить:
- ✅ Страница открывается
- ✅ Виден JSON ответ с HATEOAS ссылками
- ✅ Нет ошибок 404/500



### 2. RabbitMQ Management
URL: http://localhost:15672
Логин: guest
Пароль: guest

Что проверить:
- ✅ Интерфейс открывается
- ✅ Видны вкладки: Overview, Connections, Channels, Exchanges, Queues
- ✅ В Queues должны быть очереди для сервисов

### 3. Prometheus (Метрики)
URL: http://localhost:9091

Что проверить:
- ✅ Интерфейс Prometheus открывается
- ✅ В Status → Targets все endpoints в состоянии UP
- ✅ Можно выполнить запрос: http_server_requests_seconds_count

### 4. Grafana (Визуализация метрик)
URL: http://localhost:3000
Логин: admin
Пароль: admin

Что проверить:
- ✅ Grafana открывается и просит сменить пароль
- ✅ В разделе Dashboards есть дашборды
- ✅ Метрики отображаются (графики HTTP запросов, JVM метрики)

### 5. Zipkin (Трассировка)
URL: http://localhost:9411

Что проверить:
- ✅ Интерфейс открывается
- ✅ Есть возможность поиска трассировок

### 6. Jenkins (CI/CD)
URL: http://localhost:8088

Что проверить:
- ✅ Jenkins открывается
- ✅ Можно залогиниться (admin/admin)

---

## 🔌 API ТЕСТИРОВАНИЕ

### 1. Тестирование Demo REST API

Используйте Postman, curl или браузер для следующих запросов:

#### Базовый endpoint
curl http://localhost:8081/api
Ожидаемый ответ: JSON с HATEOAS ссылками

#### Столы (Tables)
# Получить все столы
GET http://localhost:8081/api/tables

# Создать стол
POST http://localhost:8081/api/tables
Content-Type: application/json

{
  "capacity": 4,
  "tableNumber": "T001"
}

#### Клиенты (Clients)
# Получить всех клиентов
GET http://localhost:8081/api/clients

# Создать клиента
POST http://localhost:8081/api/clients
Content-Type: application/json

{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "phone": "+7-999-123-45-67"
}

#### Бронирования (Reservations)
# Получить все бронирования
GET http://localhost:8081/api/reservations

# Создать бронирование
POST http://localhost:8081/api/reservations
Content-Type: application/json

{
  "clientId": 1,
  "tableId": 1,
  "reservationDateTime": "2024-12-20T19:00:00",
  "numberOfGuests": 4,
  "specialRequests": "У окна"
}

### 2. Тестирование Reservation Price Service

#### HTTP Actuator (для проверки здоровья)
curl http://localhost:9092/actuator/health
Ожидаемый ответ: {"status":"UP"}

#### gRPC тестирование
Для тестирования gRPC используйте инструменты:
- grpcui: grpcui -plaintext localhost:9090
- grpcurl: grpcurl -plaintext localhost:9090 list

### 3. Тестирование Audit Service
curl http://localhost:8082/actuator/health
Ожидаемый ответ: {"status":"UP"}

### 4. Тестирование Notification Service
curl http://localhost:8083/actuator/health
Ожидаемый ответ:`{"status":"UP"}`

---

## 📊 МОНИТОРИНГ И МЕТРИКИ

### 1. Prometheus

#### Проверка targets
1. Откройте http://localhost:9091
2. Перейдите: Status → Targets
3. Все сервисы должны быть UP:
   - demo-rest:9091
   - reservation-price-service:9091
   - audit-service:8082
   - notification-service:8083

#### Выполнение запросов
В поле запроса введите:
http_server_requests_seconds_count
jvm_memory_used_bytes
rabbitmq_connections

### 2. Grafana

#### Настройка дашборда
1. Откройте http://localhost:3000 (admin/admin)
2. Перейдите: Dashboards → Browse
3. Создайте новый дашборд или используйте существующий

#### Добавление панелей
Добавьте следующие метрики:
- HTTP Requests: rate(http_server_requests_seconds_count[5m])
- JVM Memory: jvm_memory_used_bytes
- Response Time: http_server_requests_seconds{quantile="0.95"}

---

## 🔄 JENKINS CI/CD

### 1. Настройка Pipeline

1. Откройте http://localhost:8088
2. Залогиньтесь (admin/admin)
3. Создайте новый Pipeline:
   - Name: restaurant-microservices
   - Type: Pipeline
   - Pipeline script from SCM
   - SCM: Git
   - Repository URL: ваш Git URL
   - Script Path: Jenkinsfile

### 2. Запуск Pipeline

1. Нажмите "Build Now"
2. Следите за прогрессом в "Console Output"
3. Pipeline должен:
   - ✅ Собрать все сервисы
   - ✅ Запустить тесты
   - ✅ Проверить health checks
   - ✅ Завершиться успешно

### 3. Проверка результатов

После успешного выполнения Pipeline:
- Проверьте логи каждого этапа
- Убедитесь, что все health checks прошли
- Проверьте, что метрики собираются

---

## 🔧 ПРОВЕРКА МИКРОСЕРВИСОВ

### 1. Проверка взаимодействия сервисов

#### Создайте бронирование через API
# 1. Создать клиента
curl -X POST http://localhost:8081/api/clients \
  -H "Content-Type: application/json" \
  -d '{"name":"Тестовый Клиент","email":"test@example.com","phone":"+7-999-999-99-99"}'

# 2. Создать стол
curl -X POST http://localhost:8081/api/tables \
  -H "Content-Type: application/json" \
  -d '{"capacity":4,"tableNumber":"TEST-001"}'

# 3. Создать бронирование (должно вызвать расчет цены через gRPC)
curl -X POST http://localhost:8081/api/reservations \
  -H "Content-Type: application/json" \
  -d '{"clientId":1,"tableId":1,"reservationDateTime":"2024-12-25T20:00:00","numberOfGuests":4}'

#### Проверьте логи сервисов
# Проверьте логи demo-rest
docker compose logs demo-rest --tail 20

# Проверьте логи reservation-price-service
docker compose logs reservation-price-service --tail 20

# Проверьте логи audit-service (должен зафиксировать событие)
docker compose logs audit-service --tail 20

# Проверьте логи notification-service (должен отправить уведомление)
docker compose logs notification-service --tail 20

### 2. Проверка RabbitMQ очередей

1. Откройте http://localhost:15672 (guest/guest)
2. Перейдите во вкладку Queues
3. Должны быть очереди:
   - reservation-events
   - notification-queue
   - audit-queue

### 3. Проверка базы данных

# Подключитесь к PostgreSQL
docker compose exec postgres psql -U postgres -d restaurant_db

# Проверьте таблицы
\d
SELECT * FROM clients;
SELECT * FROM tables;
SELECT * FROM reservations;

### 4. Проверка Redis

# Подключитесь к Redis
docker compose exec redis redis-cli

# Проверьте ключи
KEYS *

---

## 🎯 ИТОГОВОЕ ТЕСТИРОВАНИЕ

### Полный чек-лист для демонстрации:

#### ✅ Функциональность
- [ ] Demo REST API работает (GET/POST запросы)
- [ ] Reservation Price Service рассчитывает цены (gRPC)
- [ ] Audit Service логирует события
- [ ] Notification Service отправляет уведомления
- [ ] RabbitMQ передает сообщения между сервисами

#### ✅ Инфраструктура
- [ ] PostgreSQL хранит данные
- [ ] Redis используется для кеширования
- [ ] Все контейнеры запущены и healthy

#### ✅ Мониторинг
- [ ] Prometheus собирает метрики со всех сервисов
- [ ] Grafana отображает метрики в дашбордах
- [ ] Zipkin показывает трассировки запросов

#### ✅ CI/CD
- [ ] Jenkins Pipeline успешно собирает проект
- [ ] Все этапы Pipeline проходят без ошибок
- [ ] Автоматические тесты выполняются

### Команды для быстрой проверки:

# 1. Статус всех сервисов
docker compose ps

# 2. Health checks всех сервисов
curl http://localhost:8081/actuator/health
curl http://localhost:9092/actuator/health
curl http://localhost:8082/actuator/health
curl http://localhost:8083/actuator/health

# 3. Проверка метрик в Prometheus
curl http://localhost:9091/api/v1/query?query=http_server_requests_seconds_count

# 4. Быстрый API тест
curl http://localhost:8081/api/tables

---

## 🆘 УСТРАНЕНИЕ НЕИСПРАВНОСТЕЙ

### Если сервис не запускается:
# Проверьте логи конкретного сервиса
docker compose logs [service-name] --tail 50

# Перезапустите сервис
docker compose restart [service-name]

### Если API возвращает ошибки:
# Проверьте health check
curl http://localhost:[port]/actuator/health

# Проверьте логи
docker compose logs [service-name] --tail 20

### Если метрики не собираются:
1. Проверьте в Prometheus: Status → Targets
2. Убедитесь, что все endpoints UP
3. Проверьте actuator endpoints: /actuator/prometheus

---

## 🎉 ГОТОВО!

Если все проверки пройдены успешно, ваше микросервисное приложение полностью настроено и работает корректно! 🚀