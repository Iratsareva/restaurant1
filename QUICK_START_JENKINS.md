# 🚀 Быстрый старт: Jenkins в Docker

## ✅ Да, вы сможете выполнить задание!

Jenkins в Docker полностью функционален и готов к работе.

---

## 📋 Пошаговая инструкция

### Шаг 1: Запуск Jenkins (2 минуты)

```bash
# В корне проекта
docker-compose up -d jenkins
```

Подождите 30-60 секунд, пока Jenkins запустится.

### Шаг 2: Откройте Jenkins

```
http://localhost:8080
```

### Шаг 3: Получите пароль администратора

```bash
docker-compose exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

Скопируйте пароль (например: `a1b2c3d4e5f6...`)

### Шаг 4: Первый вход

1. Вставьте пароль в Jenkins
2. Нажмите "Continue"
3. Выберите "Install suggested plugins"
4. Дождитесь установки (2-5 минут)
5. Создайте администратора:
   - Username: `admin`
   - Password: придумайте пароль
6. Нажмите "Save and Continue"
7. Нажмите "Start using Jenkins"

### Шаг 5: Установите дополнительные плагины

1. **Manage Jenkins** → **Manage Plugins**
2. Вкладка **Available**, установите:
   - ✅ Docker Pipeline
   - ✅ Maven Integration
3. Нажмите "Install without restart"
4. После установки перезапустите Jenkins

### Шаг 6: Настройте Maven

1. **Manage Jenkins** → **Tools**
2. В разделе **Maven**:
   - Нажмите "Add Maven"
   - Name: `Maven 3.9`
   - ✅ Install automatically
   - Version: `3.9.6`
3. Нажмите "Save"

### Шаг 7: Создайте Pipeline

1. **New Item** → имя: `restaurant-microservices`
2. Выберите **Pipeline** → **OK**

#### Если проект в Git:
- **Pipeline script from SCM**
- **SCM**: Git
- **Repository URL**: ваш Git URL
- **Script Path**: `Jenkinsfile`

#### Если проект локальный:
- **Pipeline script**
- Вставьте содержимое файла `Jenkinsfile.local`

3. Нажмите **Save**

### Шаг 8: Запустите Pipeline

1. Нажмите **Build Now**
2. Следите за выполнением в **Console Output**

---

## 🎯 Результат

После успешного выполнения:

✅ Все сервисы собраны  
✅ Все сервисы запущены через Docker Compose  
✅ Метрики доступны в Prometheus и Grafana  

**Доступные сервисы:**
- **Jenkins**: http://localhost:8080
- **Demo REST**: http://localhost:8081 (порт изменен, чтобы не конфликтовать с Jenkins)
- **Prometheus**: http://localhost:9091
- **Grafana**: http://localhost:3000 (admin/admin)
- **Zipkin**: http://localhost:9411

---

## 📚 Подробные инструкции

Для детальной настройки см.:
- `SETUP_JENKINS_STEP_BY_STEP.md` - пошаговая инструкция
- `JENKINS_DOCKER_SETUP.md` - подробное описание

---

## ⚠️ Важно

**Конфликт портов решен:**
- Jenkins: порт 8080
- Demo REST: порт 8081 (внешний), 8080 (внутренний в контейнере)

Все остальные сервисы используют свои порты без конфликтов.



