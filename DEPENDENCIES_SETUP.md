# Настройка зависимостей для переноса проекта

## 🚨 Важно: Обязательно выполнить перед запуском на новом устройстве!

## Проблема
При переносе проекта на другое устройство Maven не может найти локальные зависимости:
- `org.example.restaurant:events-contract:jar:1.0-SNAPSHOT` (используется в demo, audit-service, notification-service)
- `com.example:Restaurant:jar:0.0.1-SNAPSHOT` (используется только в demo)

Ошибка выглядит так:
```
Failed to execute goal on project audit-service: Could not resolve dependencies for project com.example:audit-service:jar:1.0.0
dependency: org.example.restaurant:events-contract:jar:1.0-SNAPSHOT (compile)
Could not find artifact org.example.restaurant:events-contract:jar:1.0-SNAPSHOT
```

## Решение
Локальные JAR файлы нужно установить в локальный Maven репозиторий устройства.

## 🚀 Быстрая установка (рекомендуется)

### Windows:
```cmd
install-dependencies.cmd
```

### Linux/Mac:
```bash
chmod +x install-dependencies.sh
./install-dependencies.sh
```

**Что делает скрипт:**
1. Устанавливает JAR файлы в локальный Maven репозиторий устройства
2. Копирует `events-contract.jar` в папки `lib/` сервисов (для обратной совместимости)
3. Подготавливает проект к сборке в Docker

## 📋 Новая архитектура зависимостей

**Docker Compose** теперь автоматически:
- Собирает `restaurant_api_contracts` и `events-contract` внутри контейнеров
- Устанавливает их в локальный Maven репозиторий контейнера
- Собирает все микросервисы без ручной подготовки

**Локальная разработка** требует предварительной установки зависимостей скриптами выше.

## 🔧 Ручная установка (альтернатива)

Если скрипты не работают, установите зависимости вручную:

### 1. Установка events-contract
```bash
cd demo
./mvnw install:install-file \
  -Dfile=lib/events-contract.jar \
  -DgroupId=org.example.restaurant \
  -DartifactId=events-contract \
  -Dversion=1.0-SNAPSHOT \
  -Dpackaging=jar
```

### 2. Установка restaurant-api-contracts
```bash
./mvnw install:install-file \
  -Dfile=lib/restaurant_api_contracts.jar \
  -DgroupId=com.example \
  -DartifactId=Restaurant \
  -Dversion=0.0.1-SNAPSHOT \
  -Dpackaging=jar
```

## ✅ Проверка установки
После установки проверьте, что файлы появились в локальном репозитории:
- `~/.m2/repository/org/example/restaurant/events-contract/1.0-SNAPSHOT/`
- `~/.m2/repository/com/example/Restaurant/0.0.1-SNAPSHOT/`

На Windows: `C:\Users\[USERNAME]\.m2\repository\...`

## ▶️ Запуск проекта
После установки зависимостей запустите проект:
```bash
docker compose up -d --build
```

## 📋 Какие сервисы используют какие зависимости

| Сервис | events-contract | restaurant-api-contracts |
|--------|----------------|------------------------|
| demo | ✅ | ✅ |
| audit-service | ✅ | ❌ |
| notification-service | ✅ | ❌ |
| reservation-price-service | ❌ | ❌ |

## ⚠️ Важные замечания
- **Всегда запускайте скрипт установки зависимостей на новом устройстве!**
- JAR файлы находятся в папке `demo/lib/`
- **Не удаляйте эти файлы** - они нужны для работы проекта
- При изменении контрактов пересоберите их и обновите JAR файлы
- Если вы работаете в команде, рассмотрите публикацию контрактов в корпоративный Maven репозиторий
